import { MovielistDirective } from './movielist.directive';

describe('MovielistDirective', () => {
  it('should create an instance', () => {
    const directive = new MovielistDirective();
    expect(directive).toBeTruthy();
  });
});
